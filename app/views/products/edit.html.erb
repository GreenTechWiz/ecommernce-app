<h1>Edit Product</h1>
<% if @product.errors.any? %>
  <div id="error-explanation">
    <h2><%= pluralize(@product.errors.count, "error") %> prevented this product from being updated:</h2>

    <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_tag("/products/#{@product.id}", method: :put) do %>
  <div class="row">
    <div class="col-md-6 form-group">
      <%= label_tag(:name, 'Enter Product:') %>
      <%= text_field_tag(:name,  @product.name, class: "form-control") %>
    </div> 

    <div class="col-md-6 form-group">
      <%= label_tag(:category_id, "Select a Product Category:") %>
      <%= select_tag(:category_id, options_from_collection_for_select(@categories, :id, :name,  @product.category_id), prompt: "Select Product Category", class: "form-control")%>
    </div>
  </div>


  <div class="form-group">
    <%= label_tag(:description, 'Enter Description') %>
    <%= text_area_tag(:description, @product.description, class: "form-control") %>
  </div>

  <div class="form-group">
    <%= label_tag(:image_tag, "Enter a URL to your product's image:") %>
    <%= text_field_tag(:image_tag,  @product.image_tag, class: "form-control") %>
  </div>

  <div class="form-group">
    <%= label_tag(:price, 'Enter Price') %>
    <%= number_field_tag(:price,  @product.price, class: "form-control") %>
  </div>

  <div class='text-center'>
    <%= submit_tag("Edit Product") %>
  </div>
<% end %>